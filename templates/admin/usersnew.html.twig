{% extends 'base.html.twig' %}

{% block body %}
    <div class="col-lg-4 box-form">
        <h2>Add new user</h2>
        {{ form_start(form) }}
        {{ form_widget(form) }}<br>
        <input type="submit" class="btn btn-success" value="Crear usuario">
        <a class="btn btn-danger" href="{{ path('app_admin_users') }}">Cancelar</a></button>
        {{ form_errors(form) }}
        {{ form_end(form) }}

    </div>
{% endblock %}